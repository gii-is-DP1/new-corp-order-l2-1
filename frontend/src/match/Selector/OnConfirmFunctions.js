export function selectAtLeastOne(selection, selectedElements){
    return selectedElements.length >= 1;
}

export function selectAtK(selection, selectedElements){
    return selectedElements.length >= 1;
}
