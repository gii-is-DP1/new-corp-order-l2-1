
export default function Conglomerate(state){

    switch (state.type) {
        case
    }
    const src =
    return(
        <>
            <img src={} alt={"Image of Conglomerate of type" + state.type}/>
        </>
    );
}
