# database init, supports mysql too
database=mysql
spring.datasource.url=jdbc:mysql://localhost/newcorporder
spring.datasource.username=newcorporder
spring.datasource.password=newcorporder
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always
